#include "rotation.h"
#include <list>
#include <string>

using namespace std;

Rotation::Rotation(list<Spell> _spells){
    this->spells = _spells;
}

void Rotation::GenerateStatementsFromString(string script){
    
}